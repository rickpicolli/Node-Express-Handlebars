<div class="container">
	<div class="row">
		<div class="col-md-12">
		{{!-- 	<img src="" alt="burger" class="img-responsive center-block"> --}}
		</div>
	</div>

	<div class="col-md-12">
		<h1>Yammy Burger!</h1>
		<p>Because your day will be better after have a delicious burger</p>
	</div>

	<div class="row">
		<div class="col-md-5">
			<h4>Burgers to Eat</h4>
				<ul>
					{{#each burgers}}
	 					{{#unless this.devoured}}
	  
	    			{{/unless}}
	  			{{/each}}
				</ul>
		</div>

		<div class="col-md-2"></div>

		<div class="col-md-5">
			<h4>Eaten Burgers!</h2>
				<ul>
	  			{{#each burgers}}
	    			{{#if this.devoured}}
	     
	    			{{/if}}
	  			{{/each}}
				</ul>
		</div>
	</div>



	 <h2>Add a Yammy Burger</h2>

	 <div class="row">
	 	<div class="col-md-12">
	 		<form class="add-burger">
				<input type="text" id="burger_name" name="name">
				<button type="submit" class="btn btn-success">Add this burger</button>
			</form>
		</div>
	</div>